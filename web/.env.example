# Base URL of the FastAPI backend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Bearer token of the "logged-in" user (Alice Chen @ Brookfield — the liveliest demo)
NEXT_PUBLIC_API_TOKEN=brookfield-alice-token
