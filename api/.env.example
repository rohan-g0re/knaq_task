# Path to the provided JSON data (devices.json + sensor_messages.json)
DATA_DIR=../data
# SQLite file; delete it to force a fresh re-ingest on startup
DB_URL=sqlite:///./knaq.db
# CORS origin for the Next.js dev server
WEB_ORIGIN=http://localhost:3000
